var watdo = (function () { // eslint-disable-line
  var module = {}; // eslint-disable-line

  module.someVar = false;

  module.someFunction = function () {
    return true;
  };

  return module;
})();
